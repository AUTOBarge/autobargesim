classdef mission
    properties
        pos_matrix
        speed_vec
    end
    
    methods
        function M = mission(pos, speed)
        % MISSION Creates a mission plan.
        % M = MISSION(pos, speed) creates a mission plan matrix of shape:
        %       
        %       M = [x_1, y_1, U_1
        %                 ...
        %            x_n, y_n, U_n]
        %            
        %            where n is the number of waypoints
        %
        % For more information, see

        end
    end
end

